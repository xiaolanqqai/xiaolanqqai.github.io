# MiniServer Panel — 极简服务器管理面板

> 版本 3.2.0 | GitHub Pages 静态前端 + 服务器端 C 桥接（WebSocket ↔ TCP）

---

## 目录

1. [项目概述](#1-项目概述)
2. [整体架构 (v3.0)](#2-整体架构-v30)
3. [目录结构](#3-目录结构)
4. [模块详解](#4-模块详解)
   - [4.1 C 桥接程序 (bridge.c)](#41-c-桥接程序-bridgec)
   - [4.2 SSH 协议实现（TypeScript）](#42-ssh-协议实现typescript)
   - [4.3 WebSocket 传输层](#43-websocket-传输层)
   - [4.4 GitHub Pages 前端界面](#44-github-pages-前端界面)
5. [关键类与函数说明](#5-关键类与函数说明)
6. [数据流与通信协议](#6-数据流与通信协议)
7. [依赖关系](#7-依赖关系)
8. [运行方式](#8-运行方式)
9. [v2.0 → v3.0 迁移说明](#9-v20--v30-迁移说明)
10. [毕业论文初稿](#10-毕业论文初稿)

---

## 1. 项目概述

**MiniServer Panel v3.0** 是一个面向低配 VPS（≥100MB RAM）的极简远程服务器管理面板。

- **前端层**：纯静态网页部署在 GitHub Pages，手机/电脑浏览器均可访问
- **桥接层**：服务器上运行一个单文件 C 程序（< 0.5MB RAM），把 WebSocket 流量透传到 `localhost:22`
- **协议层**：纯 TypeScript 实现的 SSH-2 协议栈，运行在浏览器中

与 v2.0 相比，v3.0 彻底移除了 Chrome/Edge 扩展与 Native Messaging Host，改用**一个零依赖的 C 桥接程序**完成 WebSocket 到 TCP 的中转。手机浏览器、桌面浏览器、Edge/Chrome/Firefox/Safari 全部通用，无需安装任何插件。

### 核心特性

| 特性 | 说明 |
|------|------|
| 跨平台访问 | 任何现代浏览器（含手机）均可使用，无需安装扩展 |
| 极低占用 | C bridge 单线程 epoll，内存 < 0.5MB，适配 100MB RAM 服务器 |
| 零外部依赖 | bridge.c 内联 SHA-1/Base64，仅需 glibc + gcc 编译 |
| 一键安装 | install.sh 自动检测系统、下载、编译、启动 |
| Token 准入 | URL 路径携带 Token，简单防扫描 |
| 强制本地化 | bridge 仅允许连接 `localhost`，防开放代理 |
| SSH 终端 | 基于 xterm.js 的 Web 终端，支持 PTY 与窗口大小自适应 |
| 仪表盘 | 实时显示 CPU、内存、磁盘、运行时间等系统信息 |
| 文件管理 | 浏览目录、新建文件夹、重命名、删除文件/目录 |
| 站点部署 | 一键创建静态站点目录，保存部署记录 |
| 新手引导 | 侧栏内置安装引导页，含可复制的安装命令与常见问题 |
| 连接检测 | bridge 未安装时自动弹出提示，一键跳转引导页 |
| 暗色模式 | 自动跟随系统主题切换 |

### 技术亮点

- **纯浏览器端 SSH 实现**：基于 Web Crypto API 在浏览器中实现完整 SSH-2 协议栈
- **单文件 C 桥接**：bridge.c 一文件搞定 WebSocket 握手、帧解析、epoll 多路复用、TCP 转发
- **UI 与传输彻底解耦**：GitHub Pages 托管 UI，服务器只跑一个轻量桥接进程

---

## 2. 整体架构 (v3.0)

```
┌─────────────────────────────────────────────────────────────────┐
│            浏览器（手机/电脑，任意现代浏览器）                       │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │            GitHub Pages (静态网页 web/)                     │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │  │
│  │  │ 仪表盘    │  │  终端     │  │ 文件管理  │  │ 部署/设置 │  │  │
│  │  └──────────┘  └────┬─────┘  └──────────┘  └──────────┘  │  │
│  │                     │ app.js                               │  │
│  │  ┌──────────────────┴──────────────────────────────────┐  │  │
│  │  │       web/js/ssh-bundle.js (TypeScript SSH 客户端)    │  │  │
│  │  │  transport ← kex ← crypto ← packet                   │  │  │
│  │  │       ↓                                              │  │  │
│  │  │  sftp (SFTP 子客户端)                                 │  │  │
│  │  └──────────────────────┬───────────────────────────────┘  │  │
│  │  ┌──────────────────────┴───────────────────────────────┐  │  │
│  │  │   WsSocket (web/js/ws-socket.ts)                    │  │  │
│  │  │   → new WebSocket(ws://SERVER:PORT/TOKEN)            │  │  │
│  │  └──────────────────────┬───────────────────────────────┘  │  │
│  └─────────────────────────┼──────────────────────────────────┘  │
└────────────────────────────┼─────────────────────────────────────┘
                             │ WebSocket (JSON + Base64 帧)
                             │ ws://SERVER:PORT/TOKEN
                    ┌────────┴────────┐
                    │  服务器 (Debian)  │
                    │  bridge.c (C)    │
                    │                 │
                    │  epoll 单线程    │
                    │  - WS 握手       │
                    │  - WS 帧解析     │
                    │  - TCP 转发      │
                    │  - Token 校验    │
                    └────────┬────────┘
                             │ 仅允许 localhost TCP
                    ┌────────┴────────┐
                    │  本机 SSH 服务    │
                    │  localhost:22    │
                    └─────────────────┘
```

### 架构分层

| 层级 | 技术栈 | 位置 | 职责 |
|------|--------|------|------|
| 表示层 | HTML + CSS + xterm.js | GitHub Pages (`web/`) | UI 渲染、用户交互、终端模拟 |
| 应用层 | JavaScript (`app.js`) | GitHub Pages (`web/`) | 视图管理、连接管理、仪表盘数据采集 |
| SSH 协议层 | TypeScript | `web/js/ssh-bundle.js`（已打包） | 完整 SSH-2 协议实现 |
| 传输层 | TypeScript (`web/js/ws-socket.ts`) | 编译到 `web/js/ssh-bundle.js` | WebSocket 客户端封装 |
| 桥接层 | C (`bridge.c`) | 服务器 (Debian) | WebSocket ↔ TCP 透传，单线程 epoll |

---

## 3. 目录结构

```
极简服务器面板/
├── bridge.c                     # ★ v3.0 核心：C 单文件 WebSocket-TCP 桥接
├── install.sh                   # ★ v3.2 一键安装脚本（自动检测系统、下载、编译、启动）
├── web/                         # GitHub Pages 静态页面（前端 SPA）
│   ├── index.html               # 主页面（含连接表单：bridgePort/token/sshHost/sshPort）
│   ├── css/
│   │   └── style.css            # Apple 风格设计系统（含暗色模式）
│   ├── js/
│   │   ├── app.js              # 前端应用逻辑 v3.0（纯 WebSocket 模式）
│   │   ├── ssh-bundle.js       # SSH 协议栈 + WsSocket (IIFE 打包，运行时使用)
│   │   └── ws-socket.ts        # WsSocket 源码（TypeScript，参考用）
│   └── lib/
│       ├── xterm.js             # xterm.js 终端模拟器
│       ├── xterm.css            # xterm 样式
│       └── xterm-addon-fit.js   # 终端尺寸自适应插件
├── 项目简介.md                   # 本文档
├── 毕业论文初稿.md               # 毕业论文初稿（Markdown 格式，约4000字）
├── README.md                    # GitHub 仓库说明
└── .gitignore                   # Git 忽略规则
```

> SSH 协议栈源码（packet.ts / crypto.ts / kex.ts / transport.ts / sftp.ts）已编译打包进 `web/js/ssh-bundle.js`，源文件不单独保留。
> v3.0 已彻底删除 `extension/`（Chrome 扩展）与 `native/`（C# Native Host）目录；v3.2 已删除 `server/`（v1.0 Go 后端）目录。

---

## 4. 模块详解

### 4.1 C 桥接程序 (bridge.c)

**文件**: [bridge.c](file:///c:/Users/43879/Desktop/极简服务器面板/bridge.c)

这是 v3.0 的核心组件。一个**单文件 C 程序**，运行在服务器上，把浏览器发来的 WebSocket 流量透传到 `localhost:22`（SSH 服务）。

#### 设计原则

- **零外部依赖**：内联实现 SHA-1、Base64，不依赖 OpenSSL，仅需 glibc
- **单线程 epoll**：I/O 多路复用，一个线程处理所有连接，内存占用极低
- **强制本地化**：仅允许 bridge 连接 `localhost`，杜绝开放代理风险
- **Token 准入**：URL 路径 `/TOKEN` 做简单防扫描

#### 编译与运行

```bash
# 在服务器（Debian）上：
gcc -O2 -o bridge bridge.c
# 运行（默认端口 8022，自动生成 Token）：
./bridge
# 自定义端口与 Token：
./bridge 8022 mySecretToken123
```

运行后控制台输出形如：
```
[bridge] listening on :8022  token=8f3a9b2c4e
```

#### 核心常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `MAX_EVENTS` | 64 | epoll 单次返回最大事件数 |
| `BUF_SIZE` | 65536 | 单次读写缓冲区 |
| `MAX_CLIENTS` | 16 | 同时最大客户端数 |
| `WS_GUID` | `258EAFA5-...` | WebSocket 握手 GUID |

#### 关键函数

| 函数 | 说明 |
|------|------|
| `sha1_init / sha1_block / sha1_final` | 内联 SHA-1 实现，用于 WebSocket 握手签名 |
| `sha1_base64(key, out)` | 计算 `SHA1(Sec-WebSocket-Key + GUID)` 并 Base64 编码 |
| `base64_encode / base64_decode` | 内联 Base64 实现，用于二进制数据传输 |
| `handle_ws_handshake(fd, req)` | 解析 HTTP 升级请求，返回 `101 Switching Protocols` |
| `ws_parse_frame(...)` | 解析 WebSocket 帧（文本/二进制/Close/Ping/Pong） |
| `ws_send_frame(fd, data, len, opcode)` | 发送 WebSocket 帧 |
| `connect_tcp(host, port)` | 创建 TCP 连接（仅允许 localhost） |
| `handle_ws_message(client, msg)` | 解析 JSON 消息，分发 connect/data/disconnect |
| `handle_ws_data(client, data)` | 浏览器 → SSH 方向：解码 Base64 后写入 TCP |
| `handle_tcp_data(client, buf, len)` | SSH → 浏览器方向：Base64 编码后发送 WS 帧 |
| `epoll_loop()` | 主事件循环 |

#### 通信协议（与浏览器端对称）

WebSocket URL：`ws://SERVER:PORT/TOKEN`

消息格式（JSON + Base64）：

| 浏览器 → bridge | 含义 |
|----------------|------|
| `{ "type": "connect", "host": "127.0.0.1", "port": "22" }` | 创建 TCP 连接 |
| `{ "type": "data", "data": "<base64>" }` | 发送二进制数据 |
| `{ "type": "disconnect" }` | 主动断开 |

| bridge → 浏览器 | 含义 |
|----------------|------|
| `{ "type": "connected" }` | TCP 连接成功 |
| `{ "type": "data", "data": "<base64>" }` | 接收二进制数据 |
| `{ "type": "disconnected" }` | 远端断开 |
| `{ "type": "error", "message": "..." }` | 错误信息 |

#### 资源占用

- 内存：稳定 < 0.5MB（每客户端约 64KB 缓冲区）
- CPU：空闲 0%，活跃时仅 I/O 等待
- 磁盘：可执行文件约 30KB（strip 后）
- 适配 100MB RAM 的 VPS

#### 一键安装脚本（v3.2 新增）

**文件**: [install.sh](file:///c:/Users/43879/Desktop/极简服务器面板/install.sh)

为最小化服务器端部署负担，v3.2 提供 Bash 一键安装脚本，将下载源码、安装编译器、编译、启动四步合并为一行命令：

```bash
curl -fsSL https://raw.githubusercontent.com/xiaolanqqai/xiaolanqqai.github.io/master/index/Tool/web/install.sh | bash
```

脚本功能：
- **自动检测系统架构**：识别 x86_64 / aarch64 / arm64
- **自动检测包管理器**：支持 apt / yum / dnf / apk
- **自动安装 gcc**：如未安装则使用对应包管理器安装
- **自动下载源码**：从 GitHub Raw 下载 bridge.c（支持多个备用 URL）
- **自动编译启动**：`gcc -O2 -o bridge bridge.c` 编译后询问是否立即运行
- **彩色日志输出**：蓝/绿/黄/红四色进度提示

---

### 4.2 SSH 协议实现（TypeScript）

这是项目的核心模块，在浏览器中实现了完整的 SSH-2 协议栈，**零外部依赖**，仅使用 Web Crypto API。

#### 4.2.1 packet.ts — 二进制包编解码

**文件**: [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)（已打包）

实现 RFC 4253 §6 定义的 SSH 二进制包格式。

**关键函数**：

| 函数 | 签名 | 说明 |
|------|------|------|
| `encodePacket` | `(payload, blockSize) => Uint8Array` | 编码为 SSH 二进制包（含长度+填充） |
| `extractPacketLength` | `(data) => number` | 从原始数据中提取包长度 |
| `writeUint32` / `writeString` / `writeMpint` / `writeNameList` | 对应写入函数 | 编码对应类型 |
| `readUint32` / `readString` / `readMpint` / `readNameList` | 对应读取函数 | 解析对应类型 |

**SSH 二进制包格式**：
```
┌──────────────┬───────────────┬─────────┬──────────────┐
│ packet_length│ padding_length│ payload │   padding    │
│   (4 bytes)  │   (1 byte)    │ (变长)   │ (4~255 bytes)│
└──────────────┴───────────────┴─────────┴──────────────┘
```

#### 4.2.2 crypto.ts — 加密原语

**文件**: [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)（已打包）

基于 Web Crypto API (`globalThis.crypto.subtle`) 实现 SSH 所需的所有加密操作。

**关键函数**：

| 函数 | 说明 |
|------|------|
| `sha256(data)` | SHA-256 哈希 |
| `hmacSha256(key, data)` | HMAC-SHA256 消息认证码 |
| `aes128CtrEncrypt / aes128CtrDecrypt` | AES-128-CTR 加密/解密 |
| `incrementCtr(iv, count)` | 计数器模式 IV 递增 |
| `verifyRsaSha256(publicKeyDer, signature, data)` | RSA-SHA256 签名验证 |
| `dhGenerateKeyPair(p, g)` | Diffie-Hellman 密钥对生成 |
| `dhComputeSharedSecret(f, x, p)` | DH 共享密钥计算 |
| `deriveKeys(K, H, sessionId)` | 派生加密/MAC 密钥（RFC 4253 §7.2，A~F） |

**密钥派生方案**（RFC 4253 §7.2）：
```
初始 IV 客户端→服务端:   HASH(K || H || "A" || session_id)
加密密钥客户端→服务端:   HASH(K || H || "C" || session_id)
完整性密钥客户端→服务端: HASH(K || H || "E" || session_id)
（服务端→客户端对应 B / D / F）
```

#### 4.2.3 kex.ts — 密钥交换

**文件**: [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)（已打包）

实现 `diffie-hellman-group14-sha256` 密钥交换（RFC 4253 §8）。

**协商的算法套件**：

| 类别 | 算法 |
|------|------|
| 密钥交换 | `diffie-hellman-group14-sha256` |
| 主机密钥 | `ssh-rsa`, `rsa-sha2-256`, `rsa-sha2-512` |
| 加密（双向） | `aes128-ctr` |
| MAC（双向） | `hmac-sha2-256` |
| 压缩（双向） | `none` |

**DH 参数**：RFC 3526 Group 14（2048 位 MODP），生成元 g=2。

#### 4.2.4 transport.ts — SSH 传输层

**文件**: [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)（已打包）

`SSHTransport` 类管理整个 SSH 会话生命周期。

**连接流程（6 阶段）**：
```
Phase 1: 版本交换（raw bytes）→ 发送 "SSH-2.0-MiniServerPanel_1.0"
Phase 2: 切换到帧模式
Phase 3: 密钥交换（未加密）→ performKeyExchange()
Phase 4: NEWKEYS 交换
Phase 5: 激活加密（AES-128-CTR + HMAC-SHA256）
Phase 6: 用户认证（密码）
```

**公开 API**：

| 方法 | 返回值 | 说明 |
|------|--------|------|
| `connect(host, port, username, password)` | `Promise<void>` | 建立 SSH 连接 |
| `openShell(cols, rows)` | `Promise<ShellChannel>` | 打开交互式 Shell |
| `execCommand(command)` | `Promise<{stdout, stderr, exitCode}>` | 执行单条命令 |
| `openSftp()` | `Promise<SftpChannel>` | 打开 SFTP 子系统 |
| `disconnect()` | `void` | 断开连接并清理资源 |
| `isConnected()` | `boolean` | 检查连接状态 |
| `getServerVersion()` | `string` | 获取服务端 SSH 版本 |

#### 4.2.5 sftp.ts — SFTP 客户端

**文件**: [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)（已打包）

`SftpClient` 类实现 SFTP v3 协议，通过 SSH 通道传输。提供 `readdir`、`mkdir`、`rename`、`remove`、`stat` 等方法供前端文件管理模块调用。

---

### 4.3 WebSocket 传输层

**文件**: [web/js/ws-socket.ts](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ws-socket.ts)（编译后并入 [web/js/ssh-bundle.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/ssh-bundle.js)）

`WsSocket` 类实现 `ITcpSocket` 接口，是浏览器与服务器 C bridge 之间的唯一通道。

**构造函数**：

```typescript
class WsSocket implements ITcpSocket {
  constructor(bridgeUrl?: string) {
    // 默认 'ws://127.0.0.1:8022/ssh'
    // 实际使用时由 app.js 传入 ws://HOST:PORT/TOKEN
  }
}
```

**关键行为**：
- `connect(host, port)`：先建立 WebSocket，握手成功后发送 `{type:"connect", host, port:String(port)}`
- `send(data)`：将 `Uint8Array` Base64 编码后发送 `{type:"data", data}`
- `onData(cb)`：接收 `{type:"data"}` 消息，Base64 解码后回调
- 连接超时 10 秒，错误提示："连接桥接服务超时，请确认服务器上 bridge 已启动且 Token 正确"

---

### 4.4 GitHub Pages 前端界面

**文件**: [web/index.html](file:///c:/Users/43879/Desktop/极简服务器面板/web/index.html) | [web/js/app.js](file:///c:/Users/43879/Desktop/极简服务器面板/web/js/app.js)

纯静态 SPA，部署在 GitHub Pages，无需任何后端。

#### 连接表单字段（v3.0）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| 服务器地址 | — | bridge 所在服务器地址（通常与 SSH 同一台） |
| bridgePort | 8022 | C bridge 监听端口 |
| token | — | 运行 bridge 后控制台显示的 Token（必填） |
| sshHost | 127.0.0.1 | 目标 SSH 主机（默认本机） |
| sshPort | 22 | 目标 SSH 端口 |
| 用户名 | root | SSH 登录用户 |
| 密码 | — | SSH 登录密码 |

#### 连接核心逻辑（app.js）

```javascript
async function connectServer(bridgeHost, bridgePort, token, sshHost, sshPort, user, pass) {
  const bridgeUrl = `ws://${bridgeHost}:${bridgePort}/${token}`
  const socket = new MiniServerSSH.WsSocket(bridgeUrl)
  transport = new MiniServerSSH.SSHTransport(socket)
  await transport.connect(sshHost, sshPort, user, pass)
  // ...
}
```

#### 视图模块

- **仪表盘**（默认视图）：通过 SSH 执行 `top`/`df`/`uptime` 等命令解析系统状态
- **引导**：首次使用安装引导，7 步图文教程（SSH 登录 → 下载 bridge.c → 安装 gcc → 编译 → 运行取 Token → 防火墙 → 回面板连接），每步含可一键复制的命令；含常见问题 FAQ
- **终端**：基于 xterm.js 的交互式 Shell
- **文件管理**：通过 SFTP 浏览/新建/重命名/删除
- **站点部署**：创建静态站点目录并保存记录
- **设置**：管理服务器列表（含 bridgePort/token/sshHost/sshPort 字段）

#### 连接失败检测与引导跳转

当连接服务器失败且错误信息匹配 bridge 相关关键词（"桥接"、"WebSocket"、"连接超时"等）时，自动弹出提示对话框，提示用户"服务器上尚未安装 bridge 桥接程序"，点击"查看安装引导"跳转到引导页。SSH 认证失败等非 bridge 错误仍走普通 toast 提示。

---

## 5. 关键类与函数说明

### 浏览器端

| 类/函数 | 文件 | 说明 |
|---------|------|------|
| `SSHTransport` | ssh-bundle.js | SSH 客户端核心，管理会话生命周期 |
| `WsSocket` | ssh-bundle.js | WebSocket 传输层，连接 C bridge |
| `SftpClient` | ssh-bundle.js | SFTP v3 客户端 |
| `connectServer(...)` | app.js | 建立完整连接（WS → SSH），失败时检测 bridge 问题 |
| `quickConnect(server)` | app.js | 从存储的服务器记录快速连接 |
| `showBridgeError(msg)` | app.js | bridge 连接失败时弹出提示对话框 |
| `copyToClipboard(text)` | app.js | 引导页命令一键复制（含 fallback） |
| `navigate(viewName)` | app.js | 视图切换 |

### 服务器端（C bridge）

| 函数 | 说明 |
|------|------|
| `main(argc, argv)` | 解析端口/Token 参数，启动 epoll 循环 |
| `sha1_base64(key, out)` | WebSocket 握手签名 |
| `base64_encode/decode` | 二进制 ↔ Base64 |
| `handle_ws_handshake` | HTTP → WebSocket 升级 |
| `ws_parse_frame / ws_send_frame` | WebSocket 帧编解码 |
| `connect_tcp` | 创建 TCP 连接（强制 localhost） |
| `handle_ws_message` | JSON 消息分发 |
| `handle_ws_data / handle_tcp_data` | 双向数据透传 |

---

## 6. 数据流与通信协议

### 完整数据流（用户输入命令 → SSH 执行）

```
用户在 xterm.js 输入字符
    ↓
app.js: shell.write(data)
    ↓
SSHTransport: 加密为 SSH 通道数据帧
    ↓
WsSocket.send(encryptedBytes)
    ↓ Base64 编码
WebSocket 帧: { "type": "data", "data": "<base64>" }
    ↓ WebSocket 文本帧
C bridge: ws_parse_frame → handle_ws_message
    ↓ Base64 解码
bridge: write(tcp_fd, rawBytes, len)
    ↓ TCP 流
localhost:22 SSH 服务
```

### 反向数据流（SSH 输出 → 终端显示）

```
localhost:22 SSH 服务
    ↓ TCP 流
bridge: epoll_wait → handle_tcp_data
    ↓ Base64 编码
WebSocket 帧: { "type": "data", "data": "<base64>" }
    ↓ WebSocket 文本帧
WsSocket.onmessage → Base64 解码 → onData(bytes)
    ↓
SSHTransport: 解密 SSH 通道数据帧
    ↓
ShellChannel.onData → xterm.js.write()
    ↓
终端显示
```

### 连接生命周期

```
1. 浏览器：new WebSocket("ws://SERVER:PORT/TOKEN")
2. bridge：epoll 接受连接 → handle_ws_handshake（校验 Token）
3. 浏览器：WsSocket.onopen → 发送 {type:"connect", host, port}
4. bridge：connect_tcp("127.0.0.1", 22) → 返回 {type:"connected"}
5. 浏览器：SSHTransport 开始 SSH 协议握手（版本交换→KEX→认证）
6. 双向透传 SSH 加密数据流
7. 任端断开：bridge 发 {type:"disconnected"}，浏览器清理资源
```

---

## 7. 依赖关系

### 浏览器端依赖

| 依赖 | 类型 | 用途 |
|------|------|------|
| Web Crypto API | 浏览器原生 | SSH 加密（AES-CTR, HMAC, DH, RSA） |
| WebSocket API | 浏览器原生 | 与 C bridge 通信 |
| xterm.js | 第三方库（已打包到 `web/lib/`） | 终端模拟 |
| xterm-addon-fit | 第三方库（已打包到 `web/lib/`） | 终端尺寸自适应 |

### 服务器端依赖

| 依赖 | 类型 | 用途 |
|------|------|------|
| glibc | 系统库 | 标准 C 库 |
| gcc | 编译工具 | 编译 bridge.c（运行时不需要） |

> **无需 OpenSSL、无需 Python、无需 Node.js、无需任何运行时**。bridge.c 内联了 SHA-1 和 Base64 实现。

---

## 8. 运行方式

### 8.1 服务器端：安装并运行 C bridge

提供两种安装方式，**推荐使用一键安装脚本**（自动完成所有步骤）。

#### 方式 A：一键安装脚本（推荐，最省事）

在服务器上执行一行命令，自动完成下载源码、安装 gcc、编译、启动：

```bash
curl -fsSL https://raw.githubusercontent.com/xiaolanqqai/xiaolanqqai.github.io/master/index/Tool/web/install.sh | bash
```

脚本会自动检测系统架构、包管理器、gcc 是否存在，全程无需人工干预，运行完成后自动显示 Token。

#### 方式 B：源码编译（需 gcc）

适用于需要审查源码或手动控制每个步骤的场景。

```bash
# 1. 下载源码
wget -O bridge.c https://raw.githubusercontent.com/xiaolanqqai/xiaolanqqai.github.io/master/index/Tool/web/bridge.c

# 2. 安装 gcc（如未安装）
apt update && apt install -y gcc

# 3. 编译
gcc -O2 -o bridge bridge.c

# 4. 运行
./bridge
# 或自定义端口与 Token：
./bridge 8022 mySecretToken123
```

#### 后台常驻（推荐用 nohup 或 systemd）

```bash
# 简单方式：nohup
nohup ./bridge 8022 mySecretToken123 > bridge.log 2>&1 &

# 或 systemd 服务（/etc/systemd/system/bridge.service）
[Unit]
Description=MiniServer Bridge
After=network.target

[Service]
ExecStart=/root/bridge 8022 mySecretToken123
Restart=always
User=root

[Install]
WantedBy=multi-user.target
```

#### 防火墙放行

```bash
# 放行 bridge 端口（不要放行 22，SSH 仅本机访问）
iptables -A INPUT -p tcp --dport 8022 -j ACCEPT
# 或 ufw：
ufw allow 8022/tcp
```

### 8.2 前端：部署到 GitHub Pages

1. 将 `web/` 目录内容推送到 GitHub 仓库
2. 在仓库 Settings → Pages 中启用 GitHub Pages
3. 访问 `https://<username>.github.io/<repo>/`

### 8.3 浏览器使用

1. 打开 GitHub Pages 部署的网址（手机/电脑均可）
2. 填写连接表单：
   - 服务器地址：你的服务器 IP 或域名
   - bridgePort：8022（或自定义端口）
   - token：运行 bridge 后显示的 Token
   - sshHost：127.0.0.1（默认本机 SSH）
   - sshPort：22
   - 用户名/密码：SSH 登录凭据
3. 点击连接，开始使用

### 8.4 本地开发调试

```bash
# 1. 在服务器或本机启动 bridge
./bridge 8022 devToken

# 2. 本地启动静态服务器（web/ 目录）
cd web
python -m http.server 8000
# 或：npx serve .

# 3. 浏览器访问 http://localhost:8000
#    token 填 devToken，sshHost 填 127.0.0.1
```

---

## 9. v2.0 → v3.0 迁移说明

### 架构变更

| 维度 | v2.0 | v3.0 |
|------|------|------|
| 浏览器支持 | 仅 Chrome/Edge（需扩展） | 所有现代浏览器（含手机） |
| 本地依赖 | Chrome 扩展 + C# Native Host | 无 |
| 服务器依赖 | 无 | 一个 C 程序（< 0.5MB RAM） |
| 传输方式 | chrome.runtime Port + Native Messaging | WebSocket（直连 bridge） |
| 部署复杂度 | 用户需安装扩展 + 注册 Native Host | 服务器编译一个 C 文件即可 |
| 手机支持 | 不支持 | 完全支持 |

### 删除的文件

- `extension/` 整个目录（manifest.json, background.js, icons/）
- `native/` 整个目录（NativeHost.cs, MiniServerBridge.dll, setup.ps1, launcher.bat, launcher.ps1, main.go, go.mod）
- `src/chrome-socket.ts`
- `方案对比.md`
- `serve.ps1`

### 新增的文件

- `bridge.c` — C 单文件 WebSocket-TCP 桥接程序

### 修改的文件

| 文件 | 变更 |
|------|------|
| `src/ws-socket.ts` | 移除 `createSocket` 工厂函数，构造函数改为接受 `bridgeUrl` 参数 |
| `web/js/ssh-bundle.js` | 移除 `ChromeSocket` 类与 `chrome.runtime` 引用，仅导出 `SSHTransport` 与 `WsSocket` |
| `web/js/app.js` | 移除扩展检测逻辑，`connectServer` 改为接受 `bridgeHost/bridgePort/token/sshHost/sshPort` |
| `web/index.html` | 连接表单新增 `bridgePort/token/sshHost/sshPort` 字段，移除 transport-badge，更新版本号 |
| `项目简介.md` | 完全重写为 v3.0 架构 |

### 用户侧迁移步骤（v2.0 → v3.0）

1. 在服务器上 `gcc -O2 -o bridge bridge.c` 编译
2. 运行 `./bridge` 记录 Token
3. 浏览器打开 GitHub Pages，填写新的连接表单（多了 Token 字段）
4. 卸载旧的 Chrome 扩展与 Native Host（如已安装）

### v3.1 → v3.2 增量变更

| 维度 | v3.1 | v3.2 |
|------|------|------|
| 服务器安装方式 | 仅源码编译（需手动装 gcc） | 推荐一键脚本（自动装 gcc）+ 可选源码编译 |
| 部署步骤 | scp + apt install gcc + gcc 编译 + 运行（4 步） | curl \| bash（1 步） |
| 用户体验 | 手动分步操作 | 引导页一键复制命令 |

**新增文件**：
- `install.sh` — 一键安装脚本（自动检测系统、下载、编译、启动）

**修改文件**：
- `web/index.html` — 引导页突出展示一键安装方式，手动编译降级为备选
- `项目简介.md` — 同步更新

**用户侧升级步骤（v3.1 → v3.2）**：
1. 将 `install.sh` 推送到 GitHub 仓库
2. 服务器上执行 `curl -fsSL https://raw.githubusercontent.com/xiaolanqqai/xiaolanqqai.github.io/master/index/Tool/web/install.sh | bash`
3. 引导页自动展示新的安装流程

---

**v3.2 设计目标达成**：手机可用、跨浏览器、服务器占用 < 0.5MB、零运行时依赖、一键安装。

---

## 10. 毕业论文初稿

**文件**：[毕业论文初稿.md](file:///c:/Users/43879/Desktop/极简服务器面板/毕业论文初稿.md)

本项目配套的本科计算机专业毕业论文初稿，以 Markdown 格式撰写，学术正式风格，约 1.7 万字。论文围绕本项目的"浏览器端 SSH 协议栈 + 服务器端轻量 C 桥接"创新架构展开，完整记录了系统的设计原理、实现细节与测试验证。

### 论文结构

| 章节 | 内容 |
|------|------|
| 摘要 / Abstract | 中英文摘要与关键词 |
| 第一章 绪论 | 研究背景与意义、国内外研究现状（Shellinabox/Wetty/Guacamole 等对比）、研究内容与目标、论文组织结构 |
| 第二章 相关技术 | SSH 协议（RFC 4251-4254）、WebSocket（RFC 6455）、epoll I/O 多路复用、Web Crypto API、TypeScript、GitHub Pages |
| 第三章 系统需求分析 | 功能需求、非功能需求、用户角色与使用场景、系统用例图 |
| 第四章 系统设计 | 总体三层架构、C 桥接程序设计、SSH 协议栈设计、WebSocket 传输层设计、前端界面设计、通信协议设计 |
| 第五章 系统实现 | 开发环境、C 桥接程序实现、SSH 协议栈实现、WebSocket 传输层实现、前端应用实现（含关键代码片段） |
| 第六章 系统测试 | 测试环境、功能测试、性能测试、兼容性测试、安全性分析 |
| 第七章 总结与展望 | 工作总结、创新点、不足与展望 |
| 参考文献 | 22 篇参考文献，含 RFC 4251/4253/6455/6668/8332 等标准、学术论文与技术文档 |
| 致谢 | 致谢部分 |

### 关键代码片段来源

论文第五章引用了以下源码文件中的关键代码片段：

- `bridge.c`：内联 SHA-1、Base64、WebSocket 握手、强制本地化 TCP 连接、epoll 主循环
- `web/js/ssh-bundle.js`：SSH 协议栈（packet 编解码、crypto 加密原语、kex 密钥交换、transport 传输层、sftp 文件协议）
- `web/js/ws-socket.ts`：WsSocket 类 connect 方法
- `web/js/app.js`：连接管理、视图切换、终端初始化

### 更新说明

每次项目构建时将同步更新本论文初稿，确保论文内容与代码实现保持一致。论文初稿与项目简介同置于项目根目录，便于查阅与归档。
